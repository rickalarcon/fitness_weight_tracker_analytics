<% /*
Next, you will add an EJS template for the home page.
In the templates folder, create a new file named index.ejs 
and paste in the following markup.
*/%>


<h1 class="title">Fitness Weight Tracker - Analytics with Node.js</h1>

<p>Welcome to the <b>Fitness Weight Tracker - Analytics</b> !</p>

<% if ( auth.isAnonymous ) { %>
	<p><a href="/login">Click here</a> to login or create an account.</p>
<% } %>

<% if ( auth.isAuthenticated ) { %>
	<p><a href="/add">Add</a> a weight measurement or <a href="/list">view</a> your progress.</p>
<% } %>
